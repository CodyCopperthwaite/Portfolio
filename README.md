# sv

Everything you need to build a Svelte project, powered by [`sv`](https://github.com/sveltejs/cli).

## Creating a project

If you're seeing this, you've probably already done this step. Congrats!

```bash
# create a new project in the current directory
npx sv create

# create a new project in my-app
npx sv create my-app
```

## Developing

Once you've created a project and installed dependencies with `npm install` (or `pnpm install` or `yarn`), start a development server:

```bash
npm run dev

# or start the server and open the app in a new browser tab
npm run dev -- --open
```

## Building

To create a production version of your app:

```bash
npm run build
```

You can preview the production build with `npm run preview`.

> To deploy your app, you may need to install an [adapter](https://svelte.dev/docs/kit/adapters) for your target environment.

Cody Copperthwaite:

for my design /see design folder/ I made some changes by utilising the starterkits layout.
my gallery layout was close in design to the starterkit so i kept it.
after considration i chose to not go through with a side bar menu as i felt the top down bar was more appealing. what i chose to keep was my about page, I chose to write in third person as it makes the writing come across as a report rather than an overconfident self description.

i chose to feature a multitude of different disciplins that I have cultivated over the course of my career both in college and out. This not only shows the range of my skillset but shows adaptability also.

i chose to create custom background images rather than my solid colour background. this was primarily due to the solid colours making the rest of the UI very flat looking. By adding custom graphics i was able to give the UI depth and made the site look a lot more appealing and professional overall.

Compared to my original UI design, I believe my current design shows my skills, rather than simply telling the user about them. reducing cognitive load and allowing users to understand what I do without having to read an obscene wall of text.


to Libor,
I'd like to personally thank you for your help and patience with helping me through errors and understanding where to go next.
